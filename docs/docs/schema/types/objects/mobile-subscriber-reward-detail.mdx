---
id: mobile-subscriber-reward-detail
title: MobileSubscriberRewardDetail
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








Represents a mobile subscriber reward share.


```graphql
type MobileSubscriberRewardDetail {
  discoveryLocationAmount: BigInt
  subscriberId: String
}
```




### Fields

#### [<code style={{ fontWeight: 'normal' }}>MobileSubscriberRewardDetail.<b>discoveryLocationAmount</b></code>](#)<Bullet />[`BigInt`](/schema/types/scalars/big-int.mdx) <Badge class="badge badge--secondary" text="scalar"/> 
The amount of discovery location rewards.


#### [<code style={{ fontWeight: 'normal' }}>MobileSubscriberRewardDetail.<b>subscriberId</b></code>](#)<Bullet />[`String`](/schema/types/scalars/string.mdx) <Badge class="badge badge--secondary" text="scalar"/> 
The subscriber ID.






### Implemented By

[`MobileRewardShareDetail`](/schema/types/unions/mobile-reward-share-detail.mdx)  <Badge class="badge badge--secondary" text="union"/>

