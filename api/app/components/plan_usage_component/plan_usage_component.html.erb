<div class="space-y-8">
  <div>
    <h3 class="text-base text-gray-500 mb-2">Data Credits</h3>
    <div class="flex items-baseline gap-2">
      <span class="text-4xl font-semibold"><%= api_calls %></span>
      <% if api_calls_limit.present? %>
        <span class="text-2xl text-gray-400">/<%= number_with_delimiter(api_calls_limit) %></span>
      <% else %>
        <span class="text-2xl text-gray-400">/∞</span>
      <% end %>
    </div>
  </div>

  <div>
    <h3 class="text-base text-gray-500 mb-2">Historical Data Available from</h3>
    <div class="text-4xl font-semibold">April 8, 2023</div>
  </div>
</div> 
